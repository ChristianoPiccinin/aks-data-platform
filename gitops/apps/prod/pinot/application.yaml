apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: pinot-prod
  namespace: argocd
spec:
  destination:
    namespace: pinot
    server: https://kubernetes.default.svc
  project: default
  source:
    repoURL: https://github.com/sua-org/gitops.git
    targetRevision: HEAD
    path: apps/prod/pinot
  syncPolicy:
    automated:
      prune: true
      selfHeal: true