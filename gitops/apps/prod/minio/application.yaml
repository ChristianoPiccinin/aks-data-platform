apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: minio-prod
  namespace: argocd
spec:
  destination:
    namespace: minio
    server: https://kubernetes.default.svc
  project: default
  source:
    repoURL: https://github.com/ChristianoPiccinin/aks-data-platform
    targetRevision: HEAD
    path: apps/prod/minio
  syncPolicy:
    automated:
      prune: true
      selfHeal: true